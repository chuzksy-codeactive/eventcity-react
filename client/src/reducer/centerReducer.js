import * as types from '../actions/actionTypes';
const initialState = {
  center: {},
  error: null,
  loading: false
};
const centerReducer = (state = initialState, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};
